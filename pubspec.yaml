name: fengxing_tools
description: "Flutter开发的脚本管理器，适用于window系统"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.2.0
  window_manager: ^0.5.1
  cupertino_icons: ^1.0.8
  file_picker: ^10.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true

  # assets:
    # - data/scripts.json
